# Copy this file to .env.local and fill in the values for local development
# In production, set these in the Render dashboard (Environment → Add Environment Variable)

# The full URL to your deployed backend API
# Example: https://sentinal-api.onrender.com/api
# Leave blank in local dev — the Vite proxy (/api → localhost:5000) handles it automatically
VITE_API_URL=
